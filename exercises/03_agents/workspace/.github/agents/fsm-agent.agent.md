---
description: "TODO — write a specific description with trigger phrases (min 20 chars). Example: Use when adding or reviewing FSM transitions in simulator/board/fsm.py"
tools: [read, search, edit, execute, web]
---

<!-- TODO — Follow the 3-step process to build this custom agent:

STEP 1 — Front-matter (required + recommended fields)
  description:  trigger phrases Copilot uses to auto-invoke this agent
                Must be specific — include what it does and where (file/module)
  tools:        restrict to minimum needed — remove execute and web for FSM editing
                Safe set: [read, search, edit]

STEP 2 — Role declaration (first line of body)
  Tell the agent what it is and what it is responsible for.
  Example: "You are the FSM specialist for the VirtualBoard simulator."

STEP 3 — Constraints and context
  - What files it should read:   simulator/board/fsm.py
  - The FSM data structure:      fsm_data dict with 'states', 'current', 'transitions'
  - What it must NOT do:         edit app.py, exercises/, or any test files
  - Output format:               add tuples to the 'transitions' list only

💬 Ask Copilot Chat: "Write a .agent.md for a custom FSM agent that only reads and edits simulator/board/fsm.py"
See solution/ for a complete reference.
-->
